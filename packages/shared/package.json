{
  "name": "@lexeme/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc",
    "//test": "Node strips the types itself, so there is no build step and no test runner to configure.",
    "test": "node --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "ts-fsrs": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
