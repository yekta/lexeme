node_modules/
dist/
.turbo/
coverage/
.DS_Store
*.log
*.tsbuildinfo

# Secrets. .env.example is the checked-in template.
.env*
!.env.example
.env.keys

# Copied out of packages/db by the server build so the bundle can read them at
# runtime; the originals are the ones under version control.
apps/server/drizzle/

.claude
